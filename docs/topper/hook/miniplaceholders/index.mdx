---
title: MiniPlaceholders
description: Query Topper leaderboard data using MiniPlaceholders in MiniMessage format.
---
import QuerySection from '@site/src/components/QuerySection';
import { topperDefinitions } from '@site/src/components/queryDefinitions';

> [!NOTE]
> Install [MiniPlaceholders](https://modrinth.com/plugin/miniplaceholders)

The plugin provides a MiniPlaceholders placeholder to allow you to query data from the Top Holder.

## Usage

```
<topper_global:<query>>
<topper_player:<query>>
```

Check the [Query](/topper/query/) section for more information about how to set the `<query>`.

## Example

<QuerySection definitions={topperDefinitions} prefix globalPrefix="<topper_global:" globalSuffix=">" playerPrefix="<topper_player:" playerSuffix=">" />
