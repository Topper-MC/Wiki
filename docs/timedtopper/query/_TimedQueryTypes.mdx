import QueryVar from '@site/src/components/forms/QueryVar';
import QueryCode from '@site/src/components/QueryCode';

{(!props.type || props.type === 'remain_time') && (
  <div>
    {props.type ? null : (props.prefix ? <h3><code>remain_time</code></h3> : <h3 id="remain_time"><code>remain_time</code></h3>)}
    <ul>
      <li><strong>Format</strong>:
        <ul>
          <li><QueryCode p={props} type="global">&lt;holder&gt;;remain_time</QueryCode></li>
          <li><QueryCode p={props} type="global">&lt;holder&gt;;remain_time;&lt;format&gt;</QueryCode></li>
        </ul>
      </li>
      <li><strong>Description</strong>: The remaining time until the next reset.</li>
      <li><strong>Argument</strong>:
        <ul>
          <li><code>&lt;holder&gt;</code>: The name of the Holder</li>
          <li><code>&lt;format&gt;</code>: A time format pattern (e.g., <code>HH:mm:ss</code> for hours:minutes:seconds)</li>
        </ul>
      </li>
      <li><strong>Example</strong>:
        <ul>
          <li><QueryCode p={props} type="global"><QueryVar field="holder" />;remain_time</QueryCode>
            <ul>
              <li>Retrieve the remaining time in millis until the next reset of the Holder named <code><QueryVar field="holder" /></code></li>
            </ul>
          </li>
          <li><QueryCode p={props} type="global"><QueryVar field="holder" />;remain_time;HH:mm:ss</QueryCode>
            <ul>
              <li>Retrieve the remaining time until the next reset of the Holder named <code><QueryVar field="holder" /></code></li>
            </ul>
          </li>
        </ul>
      </li>
    </ul>
  </div>
)}

{(!props.type || props.type === 'end_time') && (
  <div>
    {props.type ? null : (props.prefix ? <h3><code>end_time</code></h3> : <h3 id="end_time"><code>end_time</code></h3>)}
    <ul>
      <li><strong>Format</strong>:
        <ul>
          <li><QueryCode p={props} type="global">&lt;holder&gt;;end_time</QueryCode></li>
          <li><QueryCode p={props} type="global">&lt;holder&gt;;end_time;&lt;format&gt;</QueryCode></li>
        </ul>
      </li>
      <li><strong>Description</strong>: The time of the next reset.</li>
      <li><strong>Argument</strong>:
        <ul>
          <li><code>&lt;holder&gt;</code>: The name of the Holder</li>
          <li><code>&lt;format&gt;</code>: A date-time format pattern (e.g., <code>yyyy-MM-dd HH:mm:ss</code> for year-month-day hours:minutes:seconds)</li>
        </ul>
      </li>
      <li><strong>Example</strong>:
        <ul>
          <li><QueryCode p={props} type="global"><QueryVar field="holder" />;end_time</QueryCode>
            <ul>
              <li>Retrieve the time of the next reset in epoch millis of the Holder named <code><QueryVar field="holder" /></code></li>
            </ul>
          </li>
          <li><QueryCode p={props} type="global"><QueryVar field="holder" />;end_time;yyyy-MM-dd HH:mm:ss</QueryCode>
            <ul>
              <li>Retrieve the time of the next reset of the Holder named <code><QueryVar field="holder" /></code></li>
            </ul>
          </li>
        </ul>
      </li>
    </ul>
  </div>
)}
