---
title: Query
---
import QueryTypes from '../../topper/query/_QueryTypes.mdx';
import QueryForm from '@site/src/components/forms/QueryForm';

The query system is the same as the one used in Topper, with the addition that:

- the `<holder>` is the name of the Group Holder speficied in the configuration file of GroupTopper.
- the `top_name` query returns the name of the Group
- the `top_key` query returns the UUID of the owner of the Group

Refer to the [Topper Query](/topper/query/) page for more information.

## Types

<QueryForm />

### `top_name`
<QueryTypes type="top_name" />

### `top_key`
<QueryTypes type="top_key" />

### `top_value`
<QueryTypes type="top_value" />

### `top_value_raw`
<QueryTypes type="top_value_raw" />

### `top_rank`
<QueryTypes type="top_rank" />

### `top_size`
<QueryTypes type="top_size" />

### `value`
<QueryTypes type="value" />

### `value_raw`
<QueryTypes type="value_raw" />
